# Node.js MongoDB – User Authentication & Authorization with JWT allowing only single session per user.

## Project setup

```
npm install
```

### Run

```
node server.js
```

### .env Setup

Must contain DB_URL, PORT is optional.
